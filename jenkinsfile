pipeline{
    agent any
    tools {
                 maven 'maven3'
    }
    stages{
        stage('code package'){
            steps{
                sh script: 'mvn clean package'
            }
        }
        stage('docker compose'){
            steps{
                sh script: 'docker-compose up -d'
            }
        }
    }
}
